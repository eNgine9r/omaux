# Security

OmaUX runs with the current user's permissions and does not require sudo.

The Marketplace core does not modify packaged Omarchy files. Optional `--full` integrations edit only user-owned configuration and create timestamped backups. Review `install.sh` before enabling the full profile.
